#!/bin/sh
echo "Compiling..."; 
if g++ main.cpp -o run -lX11 -lpthread -L/usr/X11/lib -L/usr/X11R6/lib
then
	./run
else
	echo "If linker error encountered, try using the commands: \n
	sudo ln -s /opt/X11/include/X11 /usr/local/include/X11 \n
	sudo apt-get install libx11-dev"; 
fi
